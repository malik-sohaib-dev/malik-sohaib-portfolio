# Public site origin, no trailing slash. Used at build time for Open Graph / Twitter
# absolute image URLs (LinkedIn, Facebook, etc. strongly prefer HTTPS).
# Example: https://your-domain.com
VITE_SITE_ORIGIN=
